// NOTE:
// This file previously contained *multiple* command handlers in a single file (Create/Update/Submit/etc.).
// Those handlers have been split into dedicated files under `Assets/Handlers/` to keep the codebase clean,
// avoid duplicate type definitions, and follow single-responsibility best practices.
//
// The old consolidated handlers are intentionally kept below for reference only, but excluded from build.
//
// If you need to revisit the old implementations, remove the `#if false` block locally and compare against
// the dedicated handler files.

#if false
// (Old consolidated handlers preserved for reference)
#endif
